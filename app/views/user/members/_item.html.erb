<% user_color = user.user_type.blank? ? "info" : (user.is_super_admin? ? "danger" : "warning") %>

  <div>
    <%= display_profile_picture(user, 180, 180) %>
  </div>
<div>
    <%= link_to user.display_name, admin_user_path(user), :remote=>true, :class=>"text-color-blue",:style=>"margin-right:5%;" %>
      <div class="text-color-gray"><%= user.display_designation %></div>
</div>



