<ul>
	<% @users.each do |user| %>
		<li><%= link_to raw("<i class=\"fa fa-plug mr-5\"></i> #{user.name}"), backdoor_entry_path(user), method: :put, class: "mt-10", data: { confirm: "Are you sure? Do you really want to logout current session and login as #{user.name}?" } %></li>
	<% end %>
</ul>